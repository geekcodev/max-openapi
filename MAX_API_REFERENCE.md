# MAX Messenger API — полный справочник

**Base URL:** `https://platform-api2.max.ru`

**Аутентификация:** заголовок `Authorization: <access_token>` (передача токена через query-параметры больше не
поддерживается)

**Важно:** Используйте домен `platform-api2.max.ru` вместо `platform-api.max.ru`. Добавьте сертификат Минцифры в список
доверенных.

**Лимиты запросов:** 30 RPS на `platform-api2.max.ru`. Максимум 2 сообщения/редактирования/удаления/ответа в секунду на
диалог/чат/канал.

---

## Содержание

### Методы

1. [GET /me](#1-get-me) — Информация о боте
2. [PATCH /me/commands](#2-patch-mecommands) — Редактирование команд бота
3. [GET /chats](#3-get-chats) — Список чатов (УСТАРЕЛО с июня 2026)
4. [GET /chats/{chatId}](#4-get-chatschatid) — Информация о чате
5. [PATCH /chats/{chatId}](#5-patch-chatschatid) — Редактирование чата
6. [POST /chats/{chatId}/actions](#6-post-chatschatidactions) — Отправка действия бота
7. [GET /chats/{chatId}/pin](#7-get-chatschatidpin) — Получить закреплённое сообщение
8. [PUT /chats/{chatId}/pin](#8-put-chatschatidpin) — Закрепить сообщение
9. [DELETE /chats/{chatId}/pin](#9-delete-chatschatidpin) — Открепить сообщение
10. [GET /chats/{chatId}/members/me](#10-get-chatschatidmembersme) — Участие бота в чате
11. [DELETE /chats/{chatId}/members/me](#11-delete-chatschatidmembersme) — Удалить бота из чата
12. [GET /chats/{chatId}/members/admins](#12-get-chatschatidmembersadmins) — Список администраторов
13. [POST /chats/{chatId}/members/admins](#13-post-chatschatidmembersadmins) — Назначить администратора
14. [DELETE /chats/{chatId}/members/admins/{userId}](#14-delete-chatschatidmembersadminsid) — Снять администратора
15. [GET /chats/{chatId}/members](#15-get-chatschatidmembers) — Список участников
16. [POST /chats/{chatId}/members](#16-post-chatschatidmembers) — Добавить участников
17. [DELETE /chats/{chatId}/members](#17-delete-chatschatidmembers) — Удалить участника
18. [GET /subscriptions](#18-get-subscriptions) — Список webhook-подписок
19. [POST /subscriptions](#19-post-subscriptions) — Создать webhook-подписку
20. [DELETE /subscriptions](#20-delete-subscriptions) — Удалить webhook-подписку
21. [GET /updates](#21-get-updates) — Long polling
22. [POST /uploads](#22-post-uploads) — Загрузка медиафайлов
23. [GET /messages](#23-get-messages) — Получить сообщения
24. [POST /messages](#24-post-messages) — Отправить сообщение
25. [PUT /messages](#25-put-messages) — Редактировать сообщение
26. [DELETE /messages](#26-delete-messages) — Удалить сообщение
27. [GET /messages/{messageId}](#27-get-messagesmessageid) — Получить сообщение по ID
28. [GET /videos/{videoToken}](#28-get-videosvideotoken) — Информация о видео
29. [POST /answers](#29-post-answers) — Ответ на callback

### Объекты

30. [User](#объект-user)
31. [UserWithPhoto](#объект-userwithphoto)
32. [BotInfo](#объект-botinfo)
33. [ChatMember](#объект-chatmember)
34. [Chat](#объект-chat)
35. [Message](#объект-message)
36. [NewMessageBody](#объект-newmessagebody)
37. [Update](#объект-update)

### Справочные разделы

38. [HTTP-коды ошибок](#http-коды-ошибок)
39. [Клавиатура для чат-ботов](#клавиатура-для-чат-ботов)
40. [Типы кнопок](#типы-кнопок)
41. [Кнопка request_contact](#кнопка-request_contact)
42. [Кнопка clipboard](#кнопка-clipboard)
43. [Форматирование текста](#форматирование-текста)
44. [Типы вложений](#типы-вложений)
45. [Правила комбинирования вложений](#правила-комбинирования-вложений)
46. [Загрузка медиафайлов](#загрузка-медиафайлов)
47. [Схема inline-клавиатуры](#схема-inline-клавиатуры)
48. [Схемы вложений](#схемы-вложений)

---

## 1. GET /me

**Информация о боте.** Возвращает `BotInfo` (наследует `UserWithPhoto`) с id бота, именем, username, last_activity_time,
описанием, аватаром и командами.

**Аутентификация:** `access_token` (apiKey)

**Ответ:**

| Поле                 | Тип                            | Описание                              |
|----------------------|--------------------------------|---------------------------------------|
| `user_id`            | integer `<int64>`              | ID пользователя/бота                  |
| `first_name`         | string                         | Отображаемое имя                      |
| `last_name`          | string Nullable optional       | Фамилия (не возвращается для ботов)   |
| `username`           | string Nullable                | Имя пользователя/никнейм              |
| `is_bot`             | boolean                        | `true`, если бот                      |
| `last_activity_time` | integer `<int64>`              | Время последней активности (Unix, мс) |
| `name`               | string Nullable                | **Устарело**                          |
| `description`        | string Nullable optional       | До 16000 символов                     |
| `avatar_url`         | string optional                | URL миниатюры аватара                 |
| `full_avatar_url`    | string optional                | URL полного аватара                   |
| `commands`           | BotCommand[] Nullable optional | До 32 команд                          |

---

## 2. PATCH /me/commands

**Редактирование команд бота.** Добавляет, редактирует или удаляет команды. Передайте пустой массив `[]`, чтобы удалить
все команды.

**Аутентификация:** `access_token` (apiKey)

**Тело запроса:**

| Поле       | Тип                   | Обязательное | Описание     |
|------------|-----------------------|--------------|--------------|
| `commands` | BotCommand[] optional | Нет          | До 32 команд |

**Ответ:**

| Поле       | Тип                   | Описание     |
|------------|-----------------------|--------------|
| `commands` | BotCommand[] optional | До 32 команд |

---

## 3. GET /chats

**Список всех групповых чатов и каналов бота.** ⚠️ **УСТАРЕЛО с июня 2026** — метод больше не поддерживается.
Используйте `POST /subscriptions`, чтобы подписаться на события `bot_added`/`bot_started` и хранить chat_id
самостоятельно.

**Аутентификация:** `access_token` (apiKey)

**Примечание:** Long Polling нельзя использовать для получения списка чатов. Вы должны собирать chat_id из
webhook-событий и вести собственное хранилище.

---

## 4. GET /chats/{chatId}

**Информация о чате или канале.**

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Ответ:** (объект Chat)

| Поле                 | Тип                                    | Описание                                                     |
|----------------------|----------------------------------------|--------------------------------------------------------------|
| `chat_id`            | integer `<int64>`                      | ID чата/канала                                               |
| `type`               | enum `ChatType`                        | `"chat"`, `"channel"` или `"dialog"`                         |
| `status`             | enum `ChatStatus`                      | `"active"`, `"removed"`, `"left"`, `"closed"`                |
| `title`              | string Nullable                        | Название чата/канала (null для диалогов)                     |
| `icon`               | object Image Nullable                  | Иконка чата                                                  |
| `last_event_time`    | integer `<int64>`                      | Время последнего события                                     |
| `participants_count` | integer `<int32>`                      | Количество участников (для диалогов всегда 2)                |
| `owner_id`           | integer `<int64>` Nullable optional    | ID владельца                                                 |
| `participants`       | object Nullable optional               | Участники с временем активности                              |
| `is_public`          | boolean                                | Публичный доступ (для диалогов всегда false)                 |
| `link`               | string Nullable optional               | Ссылка на чат                                                |
| `description`        | string Nullable                        | Описание                                                     |
| `dialog_with_user`   | object UserWithPhoto Nullable optional | Собеседник в диалоге (только для типа dialog)                |
| `messages_count`     | integer Nullable optional              | Количество сообщений                                         |
| `pinned_message`     | object Message Nullable optional       | Закреплённое сообщение (только при запросе конкретного чата) |

---

## 5. PATCH /chats/{chatId}

**Редактирование информации о чате/канале** (название, иконка, закреплённое сообщение). Бот должен быть администратором.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание |
|----------|---------------------------------------|----------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата  |

**Тело запроса:**

| Поле     | Тип                                                    | Обязательное | Описание                                                              |
|----------|--------------------------------------------------------|--------------|-----------------------------------------------------------------------|
| `icon`   | object PhotoAttachmentRequestPayload Nullable optional | Нет          | Запрос прикрепления изображения (поля взаимоисключающие)              |
| `title`  | string Nullable optional                               | Нет          | 1–200 символов                                                        |
| `pin`    | string Nullable optional                               | Нет          | ID сообщения для закрепления (для открепления используйте DELETE pin) |
| `notify` | boolean Nullable optional                              | Нет          | По умолчанию: `true`. Системное уведомление об изменении              |

**Ответ:** (объект Chat — как в GET /chats/{chatId})

---

## 6. POST /chats/{chatId}/actions

**Отправка действия бота** (печать, отправка фото/видео/аудио/файла).

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание |
|----------|---------------------------------------|----------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата  |

**Тело запроса:**

| Поле     | Тип                 | Обязательное | Описание                                                                                 |
|----------|---------------------|--------------|------------------------------------------------------------------------------------------|
| `action` | enum `SenderAction` | Да           | `"typing_on"`, `"sending_photo"`, `"sending_video"`, `"sending_audio"`, `"sending_file"` |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 7. GET /chats/{chatId}/pin

**Получить закреплённое сообщение в чате/канале.** Бот должен быть администратором.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание |
|----------|---------------------------------------|----------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата  |

**Ответ:**

| Поле      | Тип                              | Описание                                |
|-----------|----------------------------------|-----------------------------------------|
| `message` | object Message Nullable optional | Закреплённое сообщение (null, если нет) |

---

## 8. PUT /chats/{chatId}/pin

**Закрепить сообщение в чате/канале.** Бот должен быть администратором.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Тело запроса:**

| Поле         | Тип                       | Обязательное | Описание                                                      |
|--------------|---------------------------|--------------|---------------------------------------------------------------|
| `message_id` | string                    | Да           | ID сообщения для закрепления (совпадает с `Message.body.mid`) |
| `notify`     | boolean Nullable optional | Нет          | По умолчанию: `true`. Уведомить участников                    |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 9. DELETE /chats/{chatId}/pin

**Открепить сообщение в чате/канале.** Бот должен быть администратором.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 10. GET /chats/{chatId}/members/me

**Информация об участии бота** в чате/канале, включая доступные права администратора. Бот определяется по access_token.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Ответ:** (поля User + поля участия)

| Поле                 | Тип                            | Описание                                       |
|----------------------|--------------------------------|------------------------------------------------|
| `user_id`            | integer `<int64>`              | ID пользователя/бота                           |
| `first_name`         | string                         | Отображаемое имя                               |
| `last_name`          | string Nullable optional       | Фамилия                                        |
| `username`           | string Nullable                | Имя пользователя                               |
| `is_bot`             | boolean                        | `true`, если бот                               |
| `last_activity_time` | integer `<int64>`              | Время последней активности (Unix, мс)          |
| `name`               | string Nullable                | **Устарело**                                   |
| `description`        | string Nullable optional       | До 16000 символов                              |
| `avatar_url`         | string optional                | URL миниатюры аватара                          |
| `full_avatar_url`    | string optional                | URL полного аватара                            |
| `last_access_time`   | integer `<int64>`              | Последняя активность в этом чате               |
| `is_owner`           | boolean                        | Владелец чата/канала                           |
| `is_admin`           | boolean                        | Администратор чата/канала                      |
| `join_time`          | integer `<int64>`              | Дата вступления (Unix)                         |
| `permissions`        | ChatAdminPermission[] Nullable | Права администратора (только для админов)      |
| `alias`              | string optional                | Псевдоним роли, отображаемый в настройках чата |

---

## 11. DELETE /chats/{chatId}/members/me

**Удалить бота из чата/канала.**

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 12. GET /chats/{chatId}/members/admins

**Список всех администраторов** чата/канала (пользователей и ботов) с их данными. Бот должен быть администратором.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Ответ:**

| Поле      | Тип                                 | Описание                     |
|-----------|-------------------------------------|------------------------------|
| `members` | ChatMember[]                        | Список администраторов       |
| `marker`  | integer `<int64>` Nullable optional | Указатель следующей страницы |

---

## 13. POST /chats/{chatId}/members/admins

**Назначить администратора (ов).** Выдаёт права администратора пользователям/ботам, которые являются участниками чата
или подписчиками канала. Бот должен быть администратором с правом `add_admins`.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Тело запроса:**

| Поле     | Тип                                 | Обязательное | Описание                                                   |
|----------|-------------------------------------|--------------|------------------------------------------------------------|
| `admins` | ChatAdmin[]                         | Да           | Список пользователей/ботов для назначения администраторами |
| `marker` | integer `<int64>` Nullable optional | Нет          | Указатель следующей страницы                               |

Каждый `ChatAdmin` содержит:

- `user_id` — ID пользователя/бота
- `permissions` — массив строк `ChatAdminPermission`
- `alias` — строковое описание роли

**Повторный вызов метода с тем же пользователем заменяет его права (семантика PUT).**

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 14. DELETE /chats/{chatId}/members/admins/{userId}

**Снять права администратора** с пользователя/бота. Они остаются в чате. Бот должен быть администратором с правом
`add_admins`.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание                             |
|----------|---------------------------------------|--------------------------------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала                       |
| `userId` | integer `<int64>`                     | ID пользователя/бота для снятия прав |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 15. GET /chats/{chatId}/members

**Список участников** чата/канала. Бот должен быть администратором.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Query-параметры:**

| Параметр   | Тип                         | Обязательное | Описание                                                                              |
|------------|-----------------------------|--------------|---------------------------------------------------------------------------------------|
| `user_ids` | integer[] Nullable optional | Нет          | Конкретные ID пользователей для проверки (игнорирует `count` и `marker`, если заданы) |
| `marker`   | integer `<int64>` optional  | Нет          | Указатель следующей страницы                                                          |
| `count`    | integer [1-100] optional    | Нет          | По умолчанию: 20. Количество участников для возврата                                  |

**Ответ:**

| Поле      | Тип                                 | Описание                     |
|-----------|-------------------------------------|------------------------------|
| `members` | ChatMember[]                        | Список участников            |
| `marker`  | integer `<int64>` Nullable optional | Указатель следующей страницы |

---

## 16. POST /chats/{chatId}/members

**Добавить участников в групповой чат.** Бот должен быть администратором с правом `add_remove_members`. Нельзя добавлять
подписчиков в каналы.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание           |
|----------|---------------------------------------|--------------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID группового чата |

**Тело запроса:**

| Поле       | Тип       | Обязательное | Описание                |
|------------|-----------|--------------|-------------------------|
| `user_ids` | integer[] | Да           | До 100 ID пользователей |

**Ответ:**

| Поле                  | Тип                                   | Описание                                      |
|-----------------------|---------------------------------------|-----------------------------------------------|
| `success`             | boolean                               | `true` при успехе                             |
| `message`             | string optional                       | Пояснение ошибки при неудаче                  |
| `failed_user_ids`     | integer[] Nullable optional           | ID пользователей, которых не удалось добавить |
| `failed_user_details` | FailedUserDetails[] Nullable optional | Подробные причины ошибок                      |

---

## 17. DELETE /chats/{chatId}/members

**Удалить участника** из чата/канала. Бот должен быть администратором с правом `add_remove_members`.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр | Тип                                   | Описание       |
|----------|---------------------------------------|----------------|
| `chatId` | integer `<int64>` (pattern: `\-?\d+`) | ID чата/канала |

**Query-параметры:**

| Параметр  | Тип               | Обязательное | Описание                                                                                                               |
|-----------|-------------------|--------------|------------------------------------------------------------------------------------------------------------------------|
| `user_id` | integer `<int64>` | Да           | ID пользователя для удаления                                                                                           |
| `block`   | boolean optional  | Нет          | Если `true`, пользователь блокируется (только для публичных/частных чатов по ссылке, в остальных случаях игнорируется) |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 18. GET /subscriptions

**Список всех webhook-подписок.**

**Аутентификация:** `access_token` (apiKey)

**Ответ:**

| Поле            | Тип            | Описание         |
|-----------------|----------------|------------------|
| `subscriptions` | Subscription[] | Текущие подписки |

**Примечания:**

- HTTP webhook'и больше не поддерживаются (с 25 мая). Используйте HTTPS с сертификатами от доверенного CA (включая
  сертификаты Минцифры).
- Long Polling ограничен по скорости и сроку хранения событий — не подходит для production.

---

## 19. POST /subscriptions

**Создание webhook-подписки** (основной механизм доставки событий для production). Long Polling отключается, когда
подписка активна.

**Аутентификация:** `access_token` (apiKey)

### Модель доставки событий:

1. Событие возникает → POST на webhook-эндпоинт с объектом `Update`
2. TLS-валидация эндпоинта
3. Если задан `secret`, проверяется заголовок `X-Max-Bot-Api-Secret`
4. Эндпоинт должен вернуть HTTP 200 в течение 30 секунд
5. Экспоненциальные повторные попытки: 60 с → 150 с → 375 с → ... (10 попыток за ~8 часов)
6. Если успеха нет в течение 8 часов, бот автоматически отписывается

### Требования к webhook-эндпоинту:

- Только HTTPS на порту 443
- TLS-сертификат от доверенного CA или сертификат Минцифры (без самоподписных)
- Домен должен совпадать с CN или SAN
- Обязательна полная цепочка сертификатов

**Тело запроса:**

| Поле           | Тип               | Обязательное | Описание                                                                            |
|----------------|-------------------|--------------|-------------------------------------------------------------------------------------|
| `url`          | string            | Да           | URL HTTPS-эндпоинта (должен начинаться с `https://`)                                |
| `update_types` | string[] optional | Нет          | Например, `["message_created", "bot_started"]` (полный список см. в объекте Update) |
| `secret`       | string optional   | Нет          | 5–256 символов, `[a-zA-Z0-9_-]` — передаётся в заголовке `X-Max-Bot-Api-Secret`     |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 20. DELETE /subscriptions

**Отписка от webhook.** После вызова бот перестаёт получать webhook-уведомления; Long Polling становится доступным.

**Аутентификация:** `access_token` (apiKey)

**Query-параметры:**

| Параметр | Тип    | Обязательное | Описание                 |
|----------|--------|--------------|--------------------------|
| `url`    | string | Да           | URL webhook для удаления |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 21. GET /updates

**Long Polling** для получения событий. Только для разработки/тестирования — не подходит для production.

**Аутентификация:** `access_token` (apiKey)

**Query-параметры:**

| Параметр  | Тип                                 | Обязательное | Описание                                                                                                                                      |
|-----------|-------------------------------------|--------------|-----------------------------------------------------------------------------------------------------------------------------------------------|
| `limit`   | integer [1-1000] optional           | Нет          | По умолчанию: 100. Максимум обновлений в ответе                                                                                               |
| `timeout` | integer [0-90] optional             | Нет          | По умолчанию: 30. Таймаут long-polling в секундах                                                                                             |
| `marker`  | integer `<int64>` Nullable optional | Нет          | Передайте `null` или опустите для последних событий. Передайте `marker` из предыдущего ответа, чтобы получить все события с последнего чтения |
| `types`   | string[] Nullable optional          | Нет          | Например, `types=message_created,message_callback` (полный список см. в объекте Update)                                                       |

**Ответ:**

| Поле      | Тип                        | Описание                     |
|-----------|----------------------------|------------------------------|
| `updates` | Update[]                   | Список событий               |
| `marker`  | integer `<int64>` Nullable | Указатель следующей страницы |

---

## 22. POST /uploads

**Загрузка медиафайлов.** Возвращает URL для загрузки и токен для прикрепления к сообщениям.

**Аутентификация:** `access_token` (apiKey)

### Поддерживаемые типы и лимиты:

| Тип     | Форматы                              | Максимальный размер                             |
|---------|--------------------------------------|-------------------------------------------------|
| `image` | JPG, JPEG, PNG, GIF, TIFF, BMP, HEIC | 50 МБ или 7680×7680 px (оба лимита обязательны) |
| `video` | MP4, MOV, MKV, WEBM                  | 250 МБ                                          |
| `audio` | MP3, WAV, M4A и другие               | 256 МБ или 60 минут (оба лимита обязательны)    |
| `file`  | TXT, DOC, PDF и другие               | 4 ГБ                                            |

> `type=photo` устарел — используйте `type=image`.

### Домены загрузки:

- `file` → `https://fu.oneme.ru`
- `image` → `https://iu.oneme.ru`
- `video`/`audio` → `https://vu.okcdn.ru`

### Способы загрузки:

1. **Resumable-загрузка** (не multipart/form-data) — можно возобновить при сбое
2. **Multipart-загрузка** (`Content-Type: multipart/form-data`) — проще, но при сбое нужно начинать заново

**Лимит запросов:** 30 RPS на platform-api.max.ru

**Query-параметры:**

| Параметр | Тип               | Обязательное | Описание                                  |
|----------|-------------------|--------------|-------------------------------------------|
| `type`   | enum `UploadType` | Да           | `"image"`, `"video"`, `"audio"`, `"file"` |

**Ответ:**

| Поле    | Тип             | Описание                                                   |
|---------|-----------------|------------------------------------------------------------|
| `url`   | string          | URL для загрузки (без срока действия)                      |
| `token` | string optional | Токен для `attachments.payload.token` в POST/PUT /messages |

**Примечание по обработке:** после загрузки подождите перед отправкой сообщения, чтобы избежать ошибки
`attachment.not.ready`. При повторных попытках используйте экспоненциальную задержку.

---

## 23. GET /messages

**Получить сообщения** из чата или по ID сообщений.

**Аутентификация:** `access_token` (apiKey)

**Query-параметры:**

| Параметр      | Тип                        | Обязательное | Описание                                                                                                                           |
|---------------|----------------------------|--------------|------------------------------------------------------------------------------------------------------------------------------------|
| `chat_id`     | integer `<int64>` optional | Условное     | ID чата для получения сообщений (обязателен, если не задан `message_ids`). Возвращает сообщения в обратном порядке (сначала новые) |
| `message_ids` | object optional            | Условное     | ID сообщений через запятую (обязателен, если не задан `chat_id`)                                                                   |
| `from`        | integer `<int64>` optional | Нет          | Unix-время — сообщения до этого момента                                                                                            |
| `to`          | integer `<int64>` optional | Нет          | Unix-время — сообщения с этого момента                                                                                             |
| `count`       | integer [1-100] optional   | Нет          | По умолчанию: 50. Максимум сообщений для возврата                                                                                  |

**Ответ:**

| Поле       | Тип       | Описание         |
|------------|-----------|------------------|
| `messages` | Message[] | Массив сообщений |

---

## 24. POST /messages

**Отправка сообщения** в диалог, групповой чат или канал.

**Лимит запросов:** максимум 2 сообщения в секунду на диалог/чат/канал.

**Аутентификация:** `access_token` (apiKey)

**Query-параметры:**

| Параметр               | Тип                        | Обязательное | Описание                                              |
|------------------------|----------------------------|--------------|-------------------------------------------------------|
| `user_id`              | integer `<int64>` optional | Условное     | ID пользователя для сообщений в диалог                |
| `chat_id`              | integer `<int64>` optional | Условное     | ID чата для сообщений в групповой чат/канал           |
| `disable_link_preview` | boolean optional           | Нет          | Если `true`, отключает генерацию предпросмотра ссылок |

**Тело запроса:** (NewMessageBody)

| Поле          | Тип                               | Обязательное | Описание                                                                        |
|---------------|-----------------------------------|--------------|---------------------------------------------------------------------------------|
| `text`        | string Nullable                   | Нет          | До 4000 символов. Текст сообщения                                               |
| `attachments` | AttachmentRequest[] Nullable      | Нет          | `null` = без изменений; пустой массив = удалить все вложения                    |
| `link`        | object NewMessageLink Nullable    | Нет          | Ссылка на другое сообщение                                                      |
| `notify`      | boolean optional                  | Нет          | По умолчанию: `true`. Уведомить участников                                      |
| `format`      | enum TextFormat Nullable optional | Нет          | `"markdown"` или `"html"` (см. [Форматирование текста](#форматирование-текста)) |

**Ответ:**

| Поле      | Тип            | Описание               |
|-----------|----------------|------------------------|
| `message` | object Message | Отправленное сообщение |

---

## 25. PUT /messages

**Редактирование сообщения**, отправленного ботом.

**Лимит запросов:** максимум 2 редактирования в секунду на диалог/чат/канал.

### Ограничения на редактирование:

- Диалоги: сообщения с inline-клавиатурой можно редактировать в любое время; остальные — только в течение 7 дней
- Групповые чаты и каналы: любые сообщения можно редактировать в любое время

**Аутентификация:** `access_token` (apiKey)

**Query-параметры:**

| Параметр     | Тип    | Обязательное | Описание                                           |
|--------------|--------|--------------|----------------------------------------------------|
| `message_id` | string | Да           | ID сообщения для редактирования (минимум 1 символ) |

**Тело запроса:** (NewMessageBody)

| Поле          | Тип                               | Обязательное | Описание                                            |
|---------------|-----------------------------------|--------------|-----------------------------------------------------|
| `text`        | string Nullable                   | Нет          | До 4000 символов. Новый текст                       |
| `attachments` | AttachmentRequest[] Nullable      | Нет          | `null` = без изменений; пустой массив = удалить все |
| `link`        | object NewMessageLink Nullable    | Нет          | Ссылка на другое сообщение                          |
| `notify`      | boolean optional                  | Нет          | По умолчанию: `true`. Уведомить участников          |
| `format`      | enum TextFormat Nullable optional | Нет          | `"markdown"` или `"html"`                           |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 26. DELETE /messages

**Удаление сообщения** из диалога, группового чата или канала.

### Ограничения:

- Бот должен быть администратором с правом удаления
- Диалоги: можно удалять только сообщения, отправленные самим ботом
- Групповые чаты: можно удалять любые сообщения
- Каналы: можно удалять любые сообщения

**Лимит запросов:** максимум 2 удаления в секунду на диалог/чат/канал.

**Аутентификация:** `access_token` (apiKey)

**Query-параметры:**

| Параметр     | Тип    | Обязательное | Описание                                     |
|--------------|--------|--------------|----------------------------------------------|
| `message_id` | string | Да           | ID сообщения для удаления (минимум 1 символ) |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## 27. GET /messages/{messageId}

**Получить одно сообщение** по его ID.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр    | Тип                                | Описание             |
|-------------|------------------------------------|----------------------|
| `messageId` | string (pattern: `[a-zA-Z0-9_-]+`) | ID сообщения (`mid`) |

**Ответ:**

| Поле        | Тип                                    | Описание                                                                                    |
|-------------|----------------------------------------|---------------------------------------------------------------------------------------------|
| `sender`    | object User optional                   | Пользователь, отправивший сообщение                                                         |
| `recipient` | object Recipient                       | Получатель (пользователь или чат)                                                           |
| `timestamp` | integer `<int64>`                      | Время создания (Unix, мс)                                                                   |
| `link`      | object LinkedMessage Nullable optional | Пересланное или цитируемое сообщение                                                        |
| `body`      | object MessageBody                     | Содержимое сообщения (текст + вложения). Может быть null, если только пересланное сообщение |
| `stat`      | object MessageStat Nullable optional   | Статистика сообщения (только посты в каналах)                                               |
| `url`       | string Nullable optional               | Публичный URL поста в канале (не для диалогов/групп)                                        |

---

## 28. GET /videos/{videoToken}

**Информация о видео** — URL для воспроизведения и метаданные.

**Аутентификация:** `access_token` (apiKey)

**Параметры пути:**

| Параметр     | Тип                                | Описание             |
|--------------|------------------------------------|----------------------|
| `videoToken` | string (pattern: `[a-zA-Z0-9_-]+`) | Токен видео-вложения |

**Ответ:**

| Поле        | Тип                                             | Описание                                                   |
|-------------|-------------------------------------------------|------------------------------------------------------------|
| `token`     | string                                          | Токен видео-вложения                                       |
| `urls`      | object VideoUrls Nullable optional              | URL для скачивания/воспроизведения (null, если недоступны) |
| `thumbnail` | object PhotoAttachmentPayload Nullable optional | Миниатюра видео                                            |
| `width`     | integer                                         | Ширина видео                                               |
| `height`    | integer                                         | Высота видео                                               |
| `duration`  | integer                                         | Продолжительность в секундах                               |

---

## 29. POST /answers

**Ответ на callback** (нажатие кнопки). Может обновить текущее сообщение и/или отправить одноразовое уведомление.

**Лимит запросов:** максимум 2 ответа в секунду на диалог/чат/канал.

**Аутентификация:** `access_token` (apiKey)

**Query-параметры:**

| Параметр      | Тип                             | Обязательное | Описание                                                                                                      |
|---------------|---------------------------------|--------------|---------------------------------------------------------------------------------------------------------------|
| `callback_id` | string (pattern: `^(?!\s*$).+`) | Да           | ID callback'а кнопки из объекта `Update` с типом `message_callback` (поле: `updates[i].callback.callback_id`) |

**Тело запроса:**

| Поле      | Тип                                     | Обязательное | Описание                                    |
|-----------|-----------------------------------------|--------------|---------------------------------------------|
| `message` | object NewMessageBody Nullable optional | Нет          | Заполните, чтобы обновить текущее сообщение |

**Ответ:**

| Поле      | Тип             | Описание                     |
|-----------|-----------------|------------------------------|
| `success` | boolean         | `true` при успехе            |
| `message` | string optional | Пояснение ошибки при неудаче |

---

## Объекты

---

### Объект User

Базовый объект информации о пользователе/боте. Используется в цепочке наследования: User → UserWithPhoto → BotInfo /
ChatMember.

**Поля:**

| Поле                 | Тип                      | Описание                                                                                             |
|----------------------|--------------------------|------------------------------------------------------------------------------------------------------|
| `user_id`            | integer `<int64>`        | ID пользователя/бота                                                                                 |
| `first_name`         | string                   | Отображаемое имя                                                                                     |
| `last_name`          | string Nullable optional | Фамилия (не для ботов)                                                                               |
| `username`           | string Nullable          | Имя пользователя (может быть null, если недоступно)                                                  |
| `is_bot`             | boolean                  | `true`, если бот                                                                                     |
| `last_activity_time` | integer `<int64>`        | Время последней активности (Unix, мс; может отсутствовать, если пользователь скрывает онлайн-статус) |
| `name`               | string Nullable          | **Устарело**                                                                                         |

---

### Объект UserWithPhoto

Расширяет `User` — добавляет URL аватаров и описание.

**Дополнительные поля:**

| Поле              | Тип                      | Описание                                                        |
|-------------------|--------------------------|-----------------------------------------------------------------|
| `description`     | string Nullable optional | До 16000 символов. Описание пользователя (null, если не задано) |
| `avatar_url`      | string optional          | URL миниатюры аватара                                           |
| `full_avatar_url` | string optional          | URL полного аватара                                             |

---

### Объект BotInfo

Расширяет `UserWithPhoto` — добавляет команды бота. Возвращается только методом `GET /me`.

**Дополнительные поля:**

| Поле       | Тип                            | Описание          |
|------------|--------------------------------|-------------------|
| `commands` | BotCommand[] Nullable optional | До 32 команд бота |

---

### Объект ChatMember

Расширяет `UserWithPhoto` — добавляет данные об участии в чате. Возвращается методами `/chats/{chatId}/members/*`.

**Дополнительные поля:**

| Поле               | Тип                            | Описание                                                           |
|--------------------|--------------------------------|--------------------------------------------------------------------|
| `last_access_time` | integer `<int64>`              | Последняя активность в чате (может быть устаревшей для суперчатов) |
| `is_owner`         | boolean                        | Владелец чата/канала                                               |
| `is_admin`         | boolean                        | Администратор чата/канала                                          |
| `join_time`        | integer `<int64>`              | Дата вступления (Unix)                                             |
| `permissions`      | ChatAdminPermission[] Nullable | Права администратора (только для администраторов)                  |
| `alias`            | string optional                | Псевдоним роли для отображения в настройках чата                   |

**Значения enum ChatAdminPermission:**
`read_all_messages`, `add_remove_members`, `add_admins`, `change_chat_info`, `pin_message`, `write`, `can_call`,
`edit_link`, `post_edit_delete_message` (устарело → используйте `write`), `edit_message` (устарело → используйте
`edit`),
`delete_message` (устарело → используйте `delete`), `edit`, `delete`, `view_stats`

---

### Объект Chat

Общая информация о групповом чате или канале.

**Поля:**

| Поле                 | Тип                                    | Описание                                                      |
|----------------------|----------------------------------------|---------------------------------------------------------------|
| `chat_id`            | integer `<int64>`                      | ID чата/канала                                                |
| `type`               | enum `ChatType`                        | `"chat"`, `"channel"`, `"dialog"`                             |
| `status`             | enum `ChatStatus`                      | `"active"`, `"removed"`, `"left"`, `"closed"`                 |
| `title`              | string Nullable                        | Название чата (null для диалогов)                             |
| `icon`               | object Image Nullable                  | Иконка чата                                                   |
| `last_event_time`    | integer `<int64>`                      | Время последнего события                                      |
| `participants_count` | integer `<int32>`                      | Количество участников (2 для диалогов)                        |
| `owner_id`           | integer `<int64>` Nullable optional    | ID владельца                                                  |
| `participants`       | object Nullable optional               | Участники с временем активности (null при перечислении чатов) |
| `is_public`          | boolean                                | Публичный (для диалогов всегда false)                         |
| `link`               | string Nullable optional               | Ссылка на чат                                                 |
| `description`        | string Nullable                        | Описание                                                      |
| `dialog_with_user`   | object UserWithPhoto Nullable optional | Собеседник в диалоге (только для типа dialog)                 |
| `messages_count`     | integer Nullable optional              | Количество сообщений                                          |
| `pinned_message`     | object Message Nullable optional       | Закреплённое сообщение (только при запросе конкретного чата)  |

---

### Объект Message

Сообщение в чате.

**Поля:**

| Поле        | Тип                                    | Описание                                                                         |
|-------------|----------------------------------------|----------------------------------------------------------------------------------|
| `sender`    | object User optional                   | Пользователь, отправивший сообщение                                              |
| `recipient` | object Recipient                       | Получатель (пользователь или чат)                                                |
| `timestamp` | integer `<int64>`                      | Время создания (Unix, мс)                                                        |
| `link`      | object LinkedMessage Nullable optional | Пересланное или цитируемое сообщение                                             |
| `body`      | object MessageBody                     | Содержимое сообщения (текст + вложения). Null, если только пересланное сообщение |
| `stat`      | object MessageStat Nullable optional   | Статистика (только посты в каналах)                                              |
| `url`       | string Nullable optional               | Публичный URL поста в канале (не для диалогов/групп)                             |

---

### Объект NewMessageBody

Используется для создания/редактирования сообщений (POST/PUT /messages, POST /answers).

**Поля:**

| Поле          | Тип                               | Обязательное | Описание                                            |
|---------------|-----------------------------------|--------------|-----------------------------------------------------|
| `text`        | string Nullable                   | Нет          | До 4000 символов. Текст сообщения                   |
| `attachments` | AttachmentRequest[] Nullable      | Нет          | `null` = без изменений; пустой массив = удалить все |
| `link`        | object NewMessageLink Nullable    | Нет          | Ссылка на другое сообщение                          |
| `notify`      | boolean optional                  | Нет          | По умолчанию: `true`. Уведомить участников          |
| `format`      | enum TextFormat Nullable optional | Нет          | `"markdown"` или `"html"`                           |

---

### Объект Update

Описывает события в чатах/каналах. Доставляется через Webhook (POST /subscriptions) или Long Polling (GET /updates).

**Типы событий:**

| Тип события          | Описание                                                             |
|----------------------|----------------------------------------------------------------------|
| `bot_added`          | Бот добавлен в чат/канал                                             |
| `bot_started`        | Пользователь начал/возобновил диалог с ботом                         |
| `bot_stopped`        | Пользователь остановил/удалил бота (с `dialog_removed` при удалении) |
| `bot_removed`        | Бот удалён из чата/канала                                            |
| `chat_title_changed` | Изменено название чата/канала                                        |
| `dialog_cleared`     | Пользователь очистил историю диалога                                 |
| `dialog_muted`       | Пользователь отключил уведомления диалога                            |
| `dialog_unmuted`     | Пользователь включил уведомления диалога                             |
| `dialog_removed`     | Пользователь удалил диалог (также вызывает `bot_stopped`)            |
| `message_callback`   | Пользователь нажал кнопку                                            |
| `message_created`    | Новое сообщение или пост в канале                                    |
| `message_edited`     | Сообщение отредактировано                                            |
| `message_removed`    | Сообщение удалено                                                    |
| `user_added`         | Новый пользователь вступил в чат/канал                               |
| `user_removed`       | Пользователь удалён из/покинул чат/канал                             |

**Поля (пример для `bot_added`):**

| Поле          | Тип               | Описание                                               |
|---------------|-------------------|--------------------------------------------------------|
| `update_type` | string            | Тип события                                            |
| `timestamp`   | integer `<int64>` | Время события (Unix, мс)                               |
| `chat_id`     | integer `<int64>` | ID чата (где произошло событие)                        |
| `user`        | object User       | Пользователь, участвующий в событии                    |
| `is_channel`  | boolean           | `true`, если бот добавлен в канал (не в групповой чат) |

---

## HTTP-коды ошибок

| Код   | Описание                                                                                                 |
|-------|----------------------------------------------------------------------------------------------------------|
| `200` | Успешный запрос                                                                                          |
| `400` | Неверный запрос                                                                                          |
| `401` | Ошибка аутентификации                                                                                    |
| `404` | Ресурс не найден                                                                                         |
| `405` | Метод не разрешён (например, отправка сообщений в неличный диалог без включённого `bot_can_write_to_pm`) |
| `429` | Превышен лимит запросов                                                                                  |
| `503` | Сервис недоступен                                                                                        |

---

## Клавиатура для чат-ботов

Клавиатуры позволяют пользователям взаимодействовать с ботами через кнопки вместо текстовых сообщений. При создании
клавиатуры соблюдайте следующие правила раскладки:

- Текст кнопки центрируется и обрезается, если он выходит за границы кнопки
- Кнопки в одном ряду всегда одинаковой ширины
- Ширина каждого ряда равна полной ширине клавиатуры
- Все кнопки имеют одинаковую высоту по умолчанию

### Inline-клавиатура

Вы можете прикрепить inline-клавиатуру к любому сообщению бота. Она поддерживает до **210 кнопок**, сгруппированных в
**30 рядов** — до **7 кнопок в ряду** (до **3** для типов кнопок `link`, `open_app`, `request_geo_location` и
`request_contact`).

### Добавление кнопок

Чтобы добавить кнопки, отправьте сообщение через `POST /messages`. В теле запроса передайте объект `attachments` с
`type: "inline_keyboard"` и массивом `payload.buttons`.

Каждая кнопка требует параметр `text`. В зависимости от типа кнопки могут потребоваться дополнительные параметры.

**JSON-пример — одна кнопка:**

```json
{
  "text": "This message has a link button",
  "attachments": [
    {
      "type": "inline_keyboard",
      "payload": {
        "buttons": [
          [
            {
              "type": "link",
              "text": "Open site",
              "url": "https://example.com"
            }
          ]
        ]
      }
    }
  ]
}
```

---

## Типы кнопок

| Тип                    | Описание                                                                                                  |
|------------------------|-----------------------------------------------------------------------------------------------------------|
| `callback`             | Сервер MAX отправляет событие `message_callback`, если вы подписаны через Webhook или Long Polling        |
| `link`                 | Открывает ссылку в новой вкладке (максимум 2048 символов в URL)                                           |
| `request_contact`      | Запрашивает контакт и номер телефона пользователя (см. [Кнопку request_contact](#кнопка-request_contact)) |
| `request_geo_location` | Запрашивает геолокацию пользователя                                                                       |
| `open_app`             | Открывает мини-приложение                                                                                 |
| `message`              | Отправляет текстовое сообщение боту                                                                       |
| `clipboard`            | Копирует текст из свойства `payload` в буфер обмена (см. [Кнопку clipboard](#кнопка-clipboard))           |

### Свойства кнопки

| Свойство   | Тип               | Обязательное | Описание                                                                                                                                           |
|------------|-------------------|--------------|----------------------------------------------------------------------------------------------------------------------------------------------------|
| `type`     | enum `ButtonType` | Да           | Тип кнопки                                                                                                                                         |
| `text`     | string            | Да           | Текст кнопки (максимум 2048 символов, поддерживает форматирование)                                                                                 |
| `payload`  | string Nullable   | Нет          | Данные пользователя, возвращаемые в `callback`; для `clipboard` — текст для копирования; максимум 8 КБ. Если отсутствует, `callback` вернёт `null` |
| `url`      | string Nullable   | Нет          | URL ссылки (для типа `link`; максимум 2048 символов)                                                                                               |
| `intent`   | string Nullable   | Нет          | Для `request_geo_location`: `sendOnce` (одноразово) или `sendLive` (реальная геолокация)                                                           |
| `app_data` | string Nullable   | Нет          | Данные для мини-приложения (для типа `open_app`)                                                                                                   |

---

## Кнопка request_contact

Когда пользователь нажимает кнопку `request_contact`, он отправляет боту контакт своей учётной записи MAX и номер
телефона.

Сообщение с контактом содержит поле `hash`, которое позволяет проверить, что номер телефона совпадает с учётной записью
MAX пользователя. Это делает контакт пригодным для использования в качестве альтернативы авторизации.

### Схема верификации

1. Бот отправляет `POST /messages` с кнопкой `request_contact`
2. Пользователь видит кнопку **Share Contact** (Поделиться контактом)
3. Пользователь нажимает кнопку, отправляя боту номер телефона MAX
4. Бот получает контакт через `GET /messages`
5. Бот проверяет, что номер телефона совпадает с учётной записью MAX пользователя, сравнивая:

**Шаг 1** — получить значение `hash` из `attachments.payload`:

```json
{
  "payload": {
    "vcf_info": "string",
    "max_info": {},
    "hash": "string"
  },
  "type": "contact"
}
```

**Шаг 2** — вычислить `HMAC-SHA256(access_token, vcf_info)`, где:

- `HMAC-SHA256` — стандартная криптографическая функция, доступная в большинстве языков программирования
- `access_token` — токен доступа бота
- `vcf_info` — контактная информация в формате vCard

Если значения совпадают, пользователь поделился номером телефона, привязанным к его учётной записи MAX.

> **Важно:** перед хэшированием преобразуйте символы `\r\n` в `vcf_info` в реальные переносы строк.

> **Использование данных:** данные контакта, полученные через `request_contact`, можно использовать только для
> взаимодействия с текущим чат-ботом (например, регистрация в программе лояльности, проверка статуса заказа, идентификация
> в сервисе бота).

---

## Кнопка clipboard

Когда пользователь нажимает кнопку `clipboard`, текст, указанный в свойстве `payload`, копируется в его буфер обмена.

В `payload` можно передать любой текст: промокод, номер отслеживания или платёжные реквизиты.

**JSON-пример:**

```json
{
  "type": "clipboard",
  "text": "Copy",
  "payload": "123456"
}
```

---

## Форматирование текста

Текстовые сообщения в чат-ботах можно форматировать с помощью **Markdown** или **HTML**. Укажите нужное значение в поле
`format` объекта `NewMessageBody`.

### Markdown

Установите `format` в `markdown` в `NewMessageBody`.

| Отображение                        | Markdown                                                                                                             |
|------------------------------------|----------------------------------------------------------------------------------------------------------------------|
| *курсив*                           | `*emphasized*` или `_emphasized_`                                                                                    |
| **жирный**                         | `**strong**` или `__strong__`                                                                                        |
| ~~зачёркнутый~~                    | `~~strikethrough~~`                                                                                                  |
| <u>подчёркнутый</u>                | `++underline++`                                                                                                      |
| `моноширинный`                     | `` `code` `` (переносы строк внутри трактуются как пробелы)                                                          |
| [ссылка](https://dev.max.ru/)      | `[Inline URL](https://dev.max.ru/)`                                                                                  |
| @упоминание пользователя           | `"text": "[Имя Фамилия](max://user/user_id)"` (используйте полное имя из профиля MAX; если фамилии нет — только имя) |
| <font color="red">выделение</font> | `^^highlighted^^`                                                                                                    |
| заголовок                          | `# heading`                                                                                                          |
| цитата                             | `> quote`                                                                                                            |

### HTML

Установите `format` в `html` в `NewMessageBody`.

| Отображение                        | HTML                                                                                               |
|------------------------------------|----------------------------------------------------------------------------------------------------|
| *курсив*                           | `<i>` или `<em>`                                                                                   |
| **жирный**                         | `<b>` или `<strong>`                                                                               |
| ~~зачёркнутый~~                    | `<del>` или `<s>`                                                                                  |
| <u>подчёркнутый</u>                | `<ins>` или `<u>`                                                                                  |
| `моноширинный`                     | `<pre>` или `<code>`                                                                               |
| [ссылка](https://dev.max.ru/)      | `<a href="https://dev.max.ru">Docs</a>`                                                            |
| @упоминание пользователя           | `"text": "<a href=\"max://user/user_id\">Имя Фамилия</a>"` (используйте полное имя из профиля MAX) |
| <font color="red">выделение</font> | `<mark>highlighted</mark>`                                                                         |
| заголовок                          | `<h1>`, `<h2>`, `<h3>`, `<h4>` — все уровни заголовков отображаются одинаково                      |
| цитата                             | `<blockquote> Quote </blockquote>`                                                                 |

---

## Типы вложений

| Тип               | Описание                                                                                                                                 |
|-------------------|------------------------------------------------------------------------------------------------------------------------------------------|
| `image`           | Изображения — JPG, JPEG, PNG, GIF, TIFF, BMP, HEIC; максимум 50 МБ или 7680×7680 px (оба лимита обязательны)                             |
| `video`           | Видео — MP4, MOV, MKV, WEBM; максимум 250 МБ                                                                                             |
| `audio`           | Аудио — MP3, WAV, M4A и другие; максимум 256 МБ или 60 минут (оба лимита обязательны)                                                    |
| `file`            | Прочие файлы — максимум 4 ГБ; TXT, DOC, PDF и другие распространённые форматы                                                            |
| `sticker`         | Стикеры                                                                                                                                  |
| `inline_keyboard` | Кнопки клавиатуры — максимум 210 кнопок в 30 рядах, до 7 в ряду (до 3 для `link`, `open_app`, `request_geo_location`, `request_contact`) |
| `location`        | Геолокация                                                                                                                               |
| `share`           | Медиафайлы с предпросмотром                                                                                                              |

> `type=photo` больше не поддерживается. Замените `type=photo` на `type=image`.

---

## Правила комбинирования вложений

### Комбинации «Видео + изображения»

Видео и изображения можно отправлять в комбинации друг с другом и с одним вложением клавиатуры. Общее количество
вложений не должно превышать **12**.

### Комбинации с файлом

Файл можно отправлять только в комбинации с вложением клавиатуры — отправка с изображениями или видео **не
поддерживается**. К сообщению можно прикрепить только **1 файл** и **1 вложение клавиатуры**.

### Примеры допустимых комбинаций

| Комбинация                           |
|--------------------------------------|
| 6 видео, 5 изображений, 1 клавиатура |
| 12 видео                             |
| 12 изображений                       |
| 11 видео, 1 клавиатура               |
| 11 изображений, 1 клавиатура         |
| 6 видео, 6 изображений               |
| 1 файл, 1 клавиатура                 |

### JSON-примеры

**Видео, изображение, клавиатура:**

```json
{
  "text": "Message with video, image, and buttons",
  "attachments": [
    {
      "type": "video",
      "payload": {
        "token": "nJB5ncI22cjv91bDOH6vooWy3VWVhxyyD"
      }
    },
    {
      "type": "image",
      "payload": {
        "token": "4mtwu/jlqwJwSz5uYMcpHMCcn/5fqR0="
      }
    },
    {
      "type": "inline_keyboard",
      "payload": {
        "buttons": [
          [
            {
              "type": "callback",
              "text": "Button 1",
              "payload": "Button 1 pressed"
            },
            {
              "type": "callback",
              "text": "Button 2",
              "payload": "Button 2 pressed"
            },
            {
              "type": "callback",
              "text": "Button 3",
              "payload": "Button 3 pressed"
            }
          ]
        ]
      }
    }
  ]
}
```

**Файл + клавиатура:**

```json
{
  "attachments": [
    {
      "type": "file",
      "payload": {
        "token": "FILE_TOKEN"
      }
    },
    {
      "type": "inline_keyboard",
      "payload": {
        "buttons": [
          [
            {
              "type": "callback",
              "text": "Downloaded",
              "payload": "ok"
            }
          ]
        ]
      }
    }
  ]
}
```

> В сообщениях с видео или изображениями может быть только одно вложение клавиатуры.

---

## Загрузка медиафайлов

### Как это работает

Для сообщений с медиавложениями (`image`, `video`, `audio`, `file`, `share`) сначала загрузите файл с помощью
`POST /uploads`. Ответ содержит `token`, который затем используется в `attachments` запроса `POST /messages`. Один токен
соответствует одному медиафайлу.

Для изображений вместо токена можно использовать `url` — прямую ссылку на изображение в интернете. Для других типов
медиа это недоступно.

### Домены загрузки по типам

| Тип               | Домен загрузки        |
|-------------------|-----------------------|
| `file`            | `https://fu.oneme.ru` |
| `image`           | `https://iu.oneme.ru` |
| `video` / `audio` | `https://vu.okcdn.ru` |

### Способы загрузки

1. **Resumable-загрузка** — не multipart/form-data; можно возобновить при сбое
2. **Multipart-загрузка** — `Content-Type: multipart/form-data`; проще, но при сбое нужно начинать заново

### Обработка

После загрузки подождите некоторое время перед отправкой сообщения, чтобы избежать ошибки `attachment.not.ready`. При
повторных попытках используйте экспоненциальную задержку.

---

## Схема inline-клавиатуры

```json
{
  "type": "inline_keyboard",
  "payload": {
    "rows": [
      {
        "buttons": [
          {
            "type": "callback",
            "text": "Button 1",
            "payload": "data1"
          },
          {
            "type": "link",
            "text": "Open link",
            "url": "https://example.com"
          }
        ]
      },
      {
        "buttons": [
          {
            "type": "request_contact",
            "text": "Share contact"
          }
        ]
      }
    ]
  }
}
```

**Лимиты:**

- Максимум 210 кнопок всего
- Максимум 30 рядов
- Максимум 7 кнопок в ряду (максимум 3 для `link`, `open_app`, `request_geo_location`, `request_contact`)
- Текст кнопки — максимум 2048 символов (поддерживает форматирование)
- `payload` кнопки — максимум 8 КБ

---

## Схемы вложений

### ImageAttachmentPayload

| Поле    | Тип    | Описание         |
|---------|--------|------------------|
| `url`   | string | URL изображения  |
| `token` | string | Токен медиафайла |

Форматы: JPG, JPEG, PNG, GIF, TIFF, BMP, HEIC. Максимум 50 МБ или 7680×7680 px.

### VideoAttachmentPayload

| Поле       | Тип              | Описание                           |
|------------|------------------|------------------------------------|
| `url`      | object VideoUrls | URL для скачивания/воспроизведения |
| `token`    | string           | Токен медиафайла                   |
| `duration` | integer Nullable | Продолжительность в секундах       |

### AudioAttachmentPayload

| Поле       | Тип              | Описание                     |
|------------|------------------|------------------------------|
| `url`      | string           | URL аудио                    |
| `token`    | string           | Токен медиафайла             |
| `duration` | integer Nullable | Продолжительность в секундах |

### FileAttachmentPayload

| Поле        | Тип     | Описание              |
|-------------|---------|-----------------------|
| `url`       | string  | URL файла             |
| `token`     | string  | Токен медиафайла      |
| `file_size` | integer | Размер файла в байтах |

### ContactAttachmentPayload

| Поле        | Тип             | Описание                                                                                                |
|-------------|-----------------|---------------------------------------------------------------------------------------------------------|
| `vcf_info`  | string Nullable | Контактная информация в формате vCard. Преобразуйте `\r\n` в реальные переносы строк перед хэшированием |
| `vcf_phone` | string Nullable | Номер телефона контакта                                                                                 |
| `max_info`  | object Nullable | Дополнительная информация о пользователе                                                                |
| `hash`      | string          | `HMAC-SHA256(access_token, vcf_info)` — проверяет, что номер телефона совпадает с учётной записью MAX   |

### LocationAttachmentPayload

| Поле        | Тип               | Описание |
|-------------|-------------------|----------|
| `latitude`  | number `<double>` | Широта   |
| `longitude` | number `<double>` | Долгота  |

### VideoUrls

| Поле      | Тип             | Описание                |
|-----------|-----------------|-------------------------|
| `mp4`     | string Nullable | URL скачивания MP4      |
| `preview` | string Nullable | URL предпросмотра видео |
